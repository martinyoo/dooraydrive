# 현재 상태 (인수인계)

- 갱신: 2026-08-01
- 저장소: https://github.com/martinyoo/dooraydrive (PUBLIC, main)
- 작업 폴더: `C:\drive\dev\dooraydrive`

## 한 줄 요약

**M1(수동 단방향 push/pull) 구현 완료. 2주 파일럿을 시작하려는 단계에서, 사용자가 `.\SETUP.ps1`을 한 번 더 실행하면 바로 업로드 가능한 상태.**

## 지금 해야 할 일 (다음 세션의 첫 작업)

```bash
cd C:\drive\dev\dooraydrive
git pull
.\SETUP.ps1          # 프로파일 4개 init + 기준선 대조
python diag.py swstat # base 레코드 수 > 0 확인
```

그 다음 파일럿 시작:

```bash
.\dsync push -p spri2025 --dry-run   # 계획 확인
.\dsync push -p spri2025             # 실제 업로드
```

> `SETUP.ps1`이 "원격 기록이 비어 있는 프로파일이 있습니다"로 멈추면 그 프로파일의 `init`이
> 실패한 것이다. 빈 DB 상태로 `push`하면 원격에 이미 있는 파일을 신규로 오인해 **중복 업로드**한다.

## 구현 상태

| 항목 | 상태 |
|---|---|
| 코드 | `dooray_sync/` 18파일, 커밋 완료, GitHub 반영됨 |
| 테스트 | `tests/test_regressions.py` 20건 전부 통과 |
| CLI | `init` / `status` / `push` / `pull` / `reconcile` / `doctor` — 전부 `--dry-run` 지원 |
| M2(양방향·충돌·저널 복구) | 미착수 |
| M3(자동 데몬) | 미착수 |

## 파일럿 대상 (프로파일 4개)

| 프로파일 | 로컬 | 원격 | 규모 |
|---|---|---|---|
| `spri2025` | `...\WORK\spri 2025` | `WORK/spri 2025` | 700파일 / 4.2GB |
| `spri2026` | `...\WORK\spri 2026` | `WORK/spri 2026` | 623파일 / 2.3GB |
| `swstat` | `...\WORK\SW통계` | `WORK/SW통계` | 13파일 / 359MB |
| `workenv` | `...\WORK\근무환경` | `근무환경` | 104파일 / 3.6GB |

로컬 원본은 Google Drive 동기화 폴더 안에 있다. **파일럿 중에는 원본을 지우지 말 것.**

## 원격(Dooray) 현재 상태 — 주의

| 원격 폴더 | 상태 |
|---|---|
| `WORK/spri 2025` | 신규 생성됨. **테스트 중 중단된 업로드로 약 258개 파일(폴더 42)이 이미 올라가 있음** |
| `WORK/spri 2026` | 신규 생성됨, 비어 있음 |
| `WORK/SW통계` | 기존 7개 파일 (로컬과 이름·크기 동일, 내용도 MD5 일치 확인됨) |
| `근무환경` | 기존 422개 항목 |

`spri 2025`의 258개는 정상 업로드된 파일이다. `init` 후 기준선으로 잡히므로 다음 `push`는 나머지만 올린다.

## 사용자 확인 필요 사항

1. **589.5MB 동영상** (`spri 2026\과기정통부 보고\26.1 정통실 회의\video1203881183.mp4`) — Dooray Basic 플랜 한도(500MB) 초과 가능. 실패해도 파일 단위 격리라 나머지는 진행됨
2. **저장 용량** — 총 10.5GB 추가 예정. Dooray 드라이브 여유 확인 필요
3. 검토보고서 §7의 기관 확인 4건(IP ACL·플랜 한도·보안 내규·토큰 정책) 미확인

## 미해결·후속 작업

| # | 항목 | 시점 |
|---|---|---|
| 1 | 2주 파일럿 운용 후 결과 정리 (유실 0건·실패 건수·수동 실행 번거로움) | M2 착수 전 |
| 2 | PoC-07 rate limit 429 실측 (업무시간 외) | M3 착수 전 |
| 3 | PoC-06 `--kill-test` (업로드 중단 시 서버 잔존물) | M4 착수 전 |
| 4 | 부모 폴더 `updated` 발생 조건 규명 | 낮음 |
| 5 | 개인 API 토큰 만료 정책 | 운용 중 관찰 |

## 다음 세션이 반드시 지켜야 할 것

**작업 디렉터리(`C:\drive\dev\dooraydrive`) 밖에 쓰기·삭제 명령을 실행하지 말 것.**

이번 세션에서 실제로 사고가 있었다. 어시스턴트 실행 환경과 사용자 PC의 파일시스템 경계가 경로마다 다르게 동작했다 — `%APPDATA%`는 분리되어 있어 어시스턴트가 만든 설정이 사용자 PC에 반영되지 않았고, 반대로 `%LOCALAPPDATA%`는 공유되어 있어 어시스턴트의 `rm -rf`가 **사용자의 상태 DB를 실제로 삭제**했다. 그 결과 원인 진단이 세 차례 빗나갔다.

따라서:

- 설정(`%APPDATA%\dooray-sync`)·상태 DB(`%LOCALAPPDATA%\dooray-sync`)·자격 증명은 **어시스턴트가 직접 만들거나 지우지 않는다.** 사용자가 실행할 명령을 안내한다.
- 재현·실험이 필요하면 `DOORAY_SYNC_CONFIG_DIR` / `DOORAY_SYNC_STATE_DIR`를 프로젝트 폴더 하위로 지정해 격리한다(`.gitignore`에 `.dbg/` 추가되어 있음).
- 사용자 PC의 상태는 어시스턴트가 조회한 결과가 아니라 **사용자가 실행한 `python diag.py`의 출력**으로만 판단한다.

## 진단 도구

| 도구 | 용도 |
|---|---|
| `python diag.py` | 설정 경로·실행 계정·APPDATA 실경로 진단 |
| `python diag.py <프로파일>` | DB 기준선 vs 로컬 스캔 키 대조, reconcile 대상 수 |
| `.\dsync doctor -p <프로파일>` | 토큰·연결·긴 경로·DB 무결성 |

## 문서

| 파일 | 내용 |
|---|---|
| [검토보고서.md](검토보고서.md) | 타당성 결론 · API 실측(공식 문서에 없는 특성 포함) · 리스크 R1~R15 |
| [구현계획서.md](구현계획서.md) | 아키텍처 · 강제 규칙 A1~D1 · 마일스톤 · M1 결함 이력 |
| [모듈규약.md](모듈규약.md) | 모듈 인터페이스 계약(병렬 구현의 단일 진실 소스) |
| [설치안내.md](설치안내.md) | 다른 PC 설치 절차 |
| [파일럿_운영.md](파일럿_운영.md) | 2주 파일럿 운영 절차 |
| [세션_마일스톤.md](세션_마일스톤.md) | 개발 진행 실적(발표자료용) |
