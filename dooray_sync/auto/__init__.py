"""자동 동기화(M3) — 사람이 보는 cmd 창 하나로 도는 폴링 루프.

설계 요지(전체는 계획서 참조):

- **상주 데몬도, 작업 스케줄러도 쓰지 않는다.** 로그온 시 시작프로그램 폴더의
  런처가 cmd 창 하나를 최소화 상태로 띄우고, 그 안에서 러너가 폴링한다.
  굳으면 출력이 멈춘 창이 눈에 보이고 닫았다 열면 끝이다 — 숨은 프로세스의
  좀비 딜레마(락을 쥔 채 아무도 안 죽임)가 구조적으로 없다.
- **틱은 멍청하고 주기는 러너가 정한다.** 스케줄은 고정이고 '지금이 출근인가
  퇴근인가 평시인가'는 매 틱 러너가 시계를 보고 판정한다. 그래서 퇴근 시각이
  매일 달라져도 등록된 것은 아무것도 바뀌지 않는다.
- **무인 실행의 안전은 CLI 정본이 지킨다.** 러너는 `--unattended`와 삭제 상한을
  자식에게 항상 붙일 뿐, 자기만의 안전 판정을 새로 만들지 않는다.
  창이 떠 있다는 것과 사람이 본다는 것은 다르다 — 최소화해 두고 8시간 일하는
  동안 스크롤은 흐른다.
"""
from __future__ import annotations

__all__ = ["state", "launcher", "runner"]
