# dooray_sync runtime dependencies (Python 3.11+, Windows)
#   pip install -r requirements.txt
#
# ASCII ONLY in this file. pip decodes requirements files with the LOCALE
# codec (cp949 on Korean Windows) when there is no BOM, so UTF-8 Korean
# comments here crashed the colleague-PC installer with UnicodeDecodeError
# (measured 2026-08-07, byte 0xeb = first byte of a Korean char).
# Keep Korean annotations in docs/, not in this file.
#
# PoC-only minimum deps stay in poc/requirements.txt (run reproducibility).

httpx>=0.27          # HTTP client. follow_redirects=False; 307 handled manually (A2)
keyring>=25          # API token storage (OS credential manager); env vars are test-only
typer>=0.12          # CLI. Python 3.11 + `from __future__ import annotations` needs >=0.9
send2trash>=1.8      # local deletes go to the recycle bin only (C5); used by M2 delete propagation
truststore>=0.9      # use the Windows cert store, not certifi: SSL-inspecting networks inject
                     # a self-signed root CA and every request dies with CERTIFICATE_VERIFY_FAILED
