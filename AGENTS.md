# AGENTS.md

**이 저장소의 규약은 [CLAUDE.md](CLAUDE.md) 한 곳에 있습니다. 그것을 읽으세요.**

이 파일이 규약 본문을 복사해 두지 않는 이유는 실측입니다. 2026-09-14까지 두 파일은
제목과 아래 표 한 줄만 빼고 같은 내용이었는데, CLAUDE.md의 배포 전략 현황(A·C·롤백)을
고친 날 **AGENTS.md는 하루 만에 갈라졌습니다.** 옛 서술이 그대로 남아 "git 태그가 하나도
없다", "C 미구현", "롤백 수단이 없다"고 말하고 있었고 셋 다 그 시점에 이미 사실이
아니었습니다. 규약을 읽으러 온 에이전트가 하필 이 파일을 열면 틀린 것을 믿게 됩니다.

이 저장소는 같은 병을 이미 앓았습니다. 잠금 처리 정책이 `lib.ps1`과 `capture.py` 두 곳에
복제되어 각자 썩었고, 한 곳으로 모아 고쳤더니 **모은 그 파일이 다시 두 벌이 됐습니다**
(커밋 `6a7f3a9`). 거기서는 Cloud Run이 vault만 클론하므로 사본이 불가피해 갈라짐
탐지기를 만들었지만, 여기서는 사본 자체가 불필요합니다. 불필요한 사본은 만들지 않는
것이 탐지기보다 낫습니다.

## 이 파일에만 있는 것

| 항목 | 값 |
|---|---|
| vault 규약 정본 | vault의 `AGENTS.md` — 저장 전에 그것을 먼저 읽습니다 |

CLAUDE.md는 같은 자리에서 vault의 `CLAUDE.md`를 가리킵니다. 두 파일의 실질적인 차이는
이 한 줄뿐이었습니다. 그 외에는 전부 CLAUDE.md를 따르세요.
